export default function FilePage() {
  // The authenticated layout owns the persistent FileScreen. Keeping this
  // route as a marker preserves direct URLs, history, guards, and prefetching.
  return null;
}
