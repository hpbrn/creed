export default function ConnectionsPage() {
  // The authenticated layout owns the persistent screen. This route remains a
  // marker so direct URLs, history, guards, and Next.js prefetching still work.
  return null;
}
